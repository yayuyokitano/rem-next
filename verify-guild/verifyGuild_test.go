package remverifyguild

import (
	"testing"
)

func TestVerifyGuild(t *testing.T) {

	//TODO: not entirely sure how to go about automatically testing this. Probably have to inspect raw responses and mock.
	//At least this code shouldn't randomly fail, and if it does authorizeGuild probably will too. And that has tests.

}
